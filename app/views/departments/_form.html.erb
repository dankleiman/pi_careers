<%= simple_form_for(@department) do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-info btn-small' %>
  </div>
<% end %>
